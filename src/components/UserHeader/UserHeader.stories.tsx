import React from 'react'
import { UserHeader } from '@components'

const story = {
  title: 'foundation/UserHeader',
  component: UserHeader,
  argTypes: {},
}
export default story

export const Primary = () => ( 
  <UserHeader />
)

